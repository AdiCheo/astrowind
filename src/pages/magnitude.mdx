import Layout from '../layouts/Layout.astro';
import { Bar, Line, Radar } from 'react-chartjs-2';
import {
  Chart as ChartJS,
  CategoryScale,
  LinearScale,
  BarElement,
  LineElement,
  PointElement,
  RadialLinearScale,
  Title,
  Tooltip,
  Legend,
  Filler,
} from 'chart.js';

ChartJS.register(
CategoryScale,
LinearScale,
BarElement,
LineElement,
PointElement,
RadialLinearScale,
Title,
Tooltip,
Legend,
Filler
);

export const weeks = ['Week 1', 'Week 2', 'Week 3', 'Week 4', 'Week 5', 'Week 6', 'Week 7', 'Week 8', 'Week 9'];
export const ridgewayData = [16, 32, 33, 40, 27, 22, 17, 20, 16];
export const royalData = [10, 34, 24, 49, 36, 16, 33, 30, 19];

export const barOptions = {
  responsive: true,
  plugins: {
    legend: {
      position: 'top',
    },
    title: {
      display: true,
      text: 'Weekly Comparison: Ridgeway vs Royal',
    },
  },
};

export const lineOptions = {
  responsive: true,
  plugins: {
    legend: {
      position: 'top',
    },
    title: {
      display: true,
      text: 'Weekly Trends',
    },
  },
};

export const radarOptions = {
  responsive: true,
  plugins: {
    legend: {
      position: 'top',
    },
    title: {
      display: true,
      text: 'Performance Distribution',
    },
  },
  scales: {
    r: {
      min: 0,
      max: 50,
    },
  },
};

export const barData = {
  labels: weeks,
  datasets: [
    {
      label: 'Ridgeway',
      data: ridgewayData,
      backgroundColor: 'rgba(53, 162, 235, 0.5)',
    },
    {
      label: 'Royal',
      data: royalData,
      backgroundColor: 'rgba(255, 99, 132, 0.5)',
    },
  ],
};

export const lineData = {
  labels: weeks,
  datasets: [
    {
      label: 'Ridgeway',
      data: ridgewayData,
      borderColor: 'rgb(53, 162, 235)',
      backgroundColor: 'rgba(53, 162, 235, 0.5)',
      tension: 0.3,
    },
    {
      label: 'Royal',
      data: royalData,
      borderColor: 'rgb(255, 99, 132)',
      backgroundColor: 'rgba(255, 99, 132, 0.5)',
      tension: 0.3,
    },
  ],
};

export const radarData = {
  labels: weeks,
  datasets: [
    {
      label: 'Ridgeway',
      data: ridgewayData,
      borderColor: 'rgb(53, 162, 235)',
      backgroundColor: 'rgba(53, 162, 235, 0.2)',
    },
    {
      label: 'Royal',
      data: royalData,
      borderColor: 'rgb(255, 99, 132)',
      backgroundColor: 'rgba(255, 99, 132, 0.2)',
    },
  ],
};

# Performance Analysis: Ridgeway vs Royal

## Overview

This analysis compares the performance metrics between Ridgeway and Royal over a 9-week period. The data is visualized using different chart types to highlight various aspects of the comparison.

## Bar Chart Comparison

The bar chart below shows a direct week-by-week comparison between the two entities:

<div style={{ padding: '20px', maxWidth: '800px', margin: '0 auto' }}>
  <Bar options={barOptions} data={barData} />
</div>

## Trend Analysis

The line chart helps visualize the trends and patterns over time:

<div style={{ padding: '20px', maxWidth: '800px', margin: '0 auto' }}>
  <Line options={lineOptions} data={lineData} />
</div>

## Distribution Pattern

The radar chart provides a different perspective on the distribution of performance across weeks:

<div style={{ padding: '20px', maxWidth: '800px', margin: '0 auto' }}>
  <Radar options={radarOptions} data={radarData} />
</div>

## Key Findings

1. **Peak Performance**

   - Ridgeway's highest value: 40 (Week 4)
   - Royal's highest value: 49 (Week 4)

2. **Consistency**

   - Ridgeway shows more consistent performance with less variation
   - Royal demonstrates higher volatility in their numbers

3. **Overall Trends**
   - Both show similar patterns in certain weeks
   - There's a notable divergence in performance during weeks 6-7
